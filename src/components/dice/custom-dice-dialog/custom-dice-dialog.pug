page-dialog(:title="'Custom Dice'")
	.input-box
		input.number-input(type="number", min="1", max="100", step="1", ref="count", v-model.number="count")
		| D
		input.number-input(type="number", min="1", max="1000", step="1", ref="max", v-model.number="max")
	.buttons
		button.ok.commit(type="button", :disabled="!valid" @click="ok") OK
		button.cancel(type="button", @click="cancel") Cancel